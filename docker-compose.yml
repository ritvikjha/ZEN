version: '3.8'

services:
  bot:
    build: .
    container_name: discord-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      # Mount data folder so local JSON fallback data persists if needed
      - ./data:/app/data
